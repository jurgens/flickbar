
- unless visitor.blank?

  - if visitor == user
    your page
  - else
    - friendship = visitor.friendship_with user
    - if friendship.nil?
      = link_to 'Add to Friends', friendships_path(:friend_id => user.id), :method => :post, :class => 'button'
    - else
      = link_to 'Remove a Friend', friendship_path(friendship), :method => :delete, :confirm => 'Are you sure?', :class => 'button'